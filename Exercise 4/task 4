# จงเขียนโปรแกรมรับค่าตัวเลข 1 ตัว (n) จากคีย์บอร์ด จากนั้นแสดงผลตัวเลขตั้งแต่ 1 – n 
#ในบันทัดเดียวกันด้วยค าสั่ง while loop

n = int(input("Number :"))
i = 1
while i <= n:
    print(i, end = ' ')
    i = i + 1
